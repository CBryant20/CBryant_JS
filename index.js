// Create a string giving a message to the user
const message =
  "You have received this message because you have been chosen to open an important vault. Here is the secret combination:";

// Declare three variables using arithmetic operators to equal each combo number
const comboNum1 = 5 + 5;
const comboNum2 = 66 - 13 * 2;
const comboNum3 = 12 + 81 / 3;

alert(`${comboNum1} - ${comboNum2} - ${comboNum3}`);
